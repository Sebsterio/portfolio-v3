/**
 * Default scroll behavior for Next.js router navigation.
 * Set to `true` to scroll to top on navigation (Next.js default).
 * Can be overridden per-navigation via config.
 */
export const DEFAULT_IS_SCROLL = true;
