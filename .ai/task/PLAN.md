# Plan: Documentation Cleanup

1. Confirm intended design-system doc outcome because current worktree conflicts with the prior cleanup sequence.
2. Update `AGENTS.md` to make the authoritative permanent doc set explicit and deterministic.
3. Remove agent-workflow duplication from `README.md` and keep only human-entrypoint content.
4. Update `docs/ARCHITECTURE.md` with the implemented magazine slug redirect route and validation coverage caveat.
5. Update `docs/CONVENTIONS.md` with accurate validation guidance and remove tool-environment-specific advice.
6. Run docs-only validation for changed paths and claims.
