# Plan

1. Materialise the active-task files and color-system audit report.
2. Extend `theme.css` and `system/*.css` with the missing color aliases and reusable gradient/effect/decorative utilities.
3. Replace shared component and app-shell color literals with design-system utilities/tokens.
4. Replace background and project-route color literals, prioritising magazine/editorial surfaces last.
5. Validate with targeted grep checks plus `pnpm lint`, `pnpm typecheck`, and `pnpm build`.
